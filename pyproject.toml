[project]
name = "mcp-security-preflight"
version = "0.1.0"
description = "Local synthetic MCP security preflight scanner"
requires-python = ">=3.10"
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

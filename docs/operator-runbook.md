# Operator runbook

- Reset with `./scripts/reset-demo.sh`.
- Run with `./scripts/run-preflight.sh`.
- Inspect the newest Markdown and JSON report under `reports/`.
- Never replace the demo token with a real credential.
- Findings are evidence for an engineering review, not a certification.
